# 로컬 PostgreSQL 데이터베이스 계정정보
# 실제 사용시 .env 파일로 복사하여 값을 변경하세요: cp .env.example .env

LOCAL_DB_HOST=localhost
LOCAL_DB_PORT=5432
LOCAL_DB_USER=your_db_user
LOCAL_DB_PASSWORD=your_db_password
LOCAL_DB_SSLMODE=disable